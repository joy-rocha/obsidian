Para criar um ambiente virtual (.venv) no VsCode pelo terminal :

-  Criar o ambiente virtual -> `python -m venv .venv`
-  Ativar o ambiente virtual -> `.venv\Scripts\activate`
- Desativar o ambiente virtual -> `deactivate`

